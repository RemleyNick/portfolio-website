import React from "react";
import "./nav.css";

const Nav = () => {
    return (
        <nav>
            <a href="#">Home</a>
            <a href="#portfolio">Projects</a>
            <a href="#about">About</a>
            <a href="#contact">Contact</a>
        </nav>
    );
};

export default Nav;
